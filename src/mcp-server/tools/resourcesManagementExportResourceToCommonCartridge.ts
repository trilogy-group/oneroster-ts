/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { resourcesManagementExportResourceToCommonCartridge } from "../../funcs/resourcesManagementExportResourceToCommonCartridge.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExportResourceToCommonCartridgeRequest$inboundSchema,
};

export const tool$resourcesManagementExportResourceToCommonCartridge:
  ToolDefinition<typeof args> = {
    name: "resources-management-export-resource-to-common-cartridge",
    description: `Export Resource to Common Cartridge

Export a resource to Common Cartridge (.imscc) format for import into LMS systems.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await resourcesManagementExportResourceToCommonCartridge(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
